"""Live dashboard (Phase 10).

A signed magic-link (issued via the Telegram bot's ``/dashboard`` command) grants
a session cookie for a small server-rendered HTML dashboard showing the user's
applications, their statuses, and per-source counts.
"""
